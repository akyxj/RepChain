package rep.storage.test

import rep.storage._;
import rep.utils._;
import rep.protos.peer._;
import com.google.protobuf.timestamp.Timestamp

object testblockstore {
     def main(args: Array[String]): Unit = {
       
		   /*val lh = StorageMgr.GetStorageMgr("testSystem");
		   
		   val millis = TimeUtils.getCurrentTime()
       val blc = new Block(1, Option(Timestamp(millis / 1000, ((millis % 1000) * 1000000).toInt)),
        Seq.empty[Transaction],
        _root_.com.google.protobuf.ByteString.EMPTY, _root_.com.google.protobuf.ByteString.EMPTY, Seq())
		  */
		  //lh.commitAndAddBlock(blc);
		   //val bb = lh.getBlockByHash("");
		   //var blc1 = Block.parseFrom(bb);  
		   //var bci : BlockchainInfo = lh.getBlockChainInfo();
		   //printf("aa");
			
		}
}